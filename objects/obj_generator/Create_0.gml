alarm[0] = room_speed * random_range(2, 4); // mesma coisa que 60 * 2 que é a cada 2 segundos
// para randomizar o valor em determinado limite usamos o random_range()

pontos = 0;
level = 1;
plevel = 100; // Pontos por level